<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Licenta2.Views.NewRegiuneMontanaPage">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Name" FontSize="Medium" />
            <Entry Text="{Binding RegiuneMontana.Nume}" d:Text="Name of the mountain" FontSize="Small" />
            <Label Text="Location" FontSize="Medium" />
            <Entry Text="{Binding RegiuneMontana.Locatie}" d:Text="The nearest city" FontSize="Small" />
            <Label Text="Description" FontSize="Medium" />
            <Editor Text="{Binding RegiuneMontana.Descriere}" d:Text="Description" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
